[project]
name = "test-library-deployment"
version = "0.1.0"
description = "Test library deployment"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
